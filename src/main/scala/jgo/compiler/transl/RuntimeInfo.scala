package jgo.compiler
package transl

import interm._
import codeseq._
import instr._
import symbol._
import types._

object RuntimeInfo {
  val RuntimeLocation = "jgo/runtime/"
  
  val UnsignedAnnot = RuntimeLocation + "Unsigned"
}
